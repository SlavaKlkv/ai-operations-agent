[project]
name = "ai-operations-agent"
version = "0.1.0"
description = "Agentic incident-analysis system: LangGraph orchestration, MCP integrations, human-in-the-loop actions."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Slava Kulikov" }]

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy[asyncio]>=2.0.36",
    "greenlet>=3.1",
    "alembic>=1.14",
    "asyncpg>=0.30",
    "redis>=5.2",
    "langgraph>=0.2.50",
    "langchain-core>=0.3.20",
    "langchain-anthropic>=0.3.0",
    "mcp>=2.2",
    "langgraph-checkpoint-postgres>=3.0",
    "structlog>=24.4",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "aiosqlite>=0.20",
    "asgi-lifespan>=2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4", "RUF"]
# Cyrillic is a legitimate part of this codebase (Russian-language tasks in
# tests and fixtures), so the ambiguous-character checks are noise here.
ignore = [
    "RUF001", "RUF002", "RUF003",
    # `Depends(...)` in a default is how FastAPI declares dependencies.
    "B008",
]

[tool.ruff.lint.per-file-ignores]
# Alembic generates these; reformatting them makes diffs unreadable.
"migrations/versions/*" = ["E501"]
"migrations/env.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
strict = true
warn_return_any = true
